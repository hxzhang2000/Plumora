# ── 依赖安装严格模式 ────────────────────────────────────────
# 让 package.json 的 engines 真正生效：Node 版本不满足 vite 要求时
# `npm install` 直接失败，而不是装完了在构建期才报错。
# vite 7 要求 ^20.19.0 || >=22.12.0（Node 21.x、22.0–22.11 都不行）。
engine-strict=true
