# 观梅 · Plumora 目录核查报告

| 项目 | 内容 |
| --- | --- |
| 核查对象 | `D:/hxzhang/MyGithubSoftware/Plumora/`（docs/ 全量 32 个文件） |
| 核查日期 | 2026-09-25 |
| 核查方式 | 文档逐份通读 + 交叉引用比对 + **可执行验证**（core.js 用例对拍 / HTML 原型 jsdom 冒烟 / 表格列数与词表脚本核对） |
| 结论 | 骨架与算法可信，**但 v1.3/v1.4 的「互卦口径更正」没有同步到全部位置**，另有 6 处会直接导致实现错误的硬缺陷 |
| **修订状态** | ✅ **全部缺陷已于 2026-09-25 修订完毕**（详见下方「修订结果」），并已将项目纳入 git 版本控制 |

---

## 修订结果（2026-09-25 完成）

| 分类 | 条数 | 状态 |
| --- | --- | --- |
| P0 硬缺陷 | 4 | ✅ 全部修复 |
| P1 一致性缺陷 | 20 | ✅ 全部修复 |
| 结构性问题 | 4 | ✅ 全部处理（用例表双份维护已消除、版本联动改为各文档独立、术语已统一、reference 原图保留原样入库） |

**各文档版本变化**（改动的文档独立升版，未改动的不升）：

| 文档 | 修订前 | 修订后 |
| --- | --- | --- |
| docs/brand-naming.md | v1.0 | v1.1 |
| docs/dev/00-竞品调研报告.md | v1.0 | v1.1 |
| docs/dev/01-产品需求文档PRD.md | v1.2 | v1.3 |
| docs/dev/02-系统架构设计.md | v1.0 | v1.0（未改动） |
| docs/dev/03-起卦核心算法设计.md | v1.3 | v1.4 |
| docs/dev/04-数据模型与存储设计.md | v1.2 | v1.3 |
| docs/dev/05-卦象知识库设计.md | v1.3 | v1.4 |
| docs/dev/06-UI-UX设计规范.md | v1.0 | v1.1 |
| docs/dev/07-测试计划.md | v1.4 | v1.5 |
| docs/dev/08-版本计划与发布规范.md | v1.0 | v1.1 |
| docs/dev/README.md | v1.4 | v1.5 |
| docs/dev/knowledge-review/手写笔记识读记录.md | v1.1 | v1.2 |
| docs/ui/README.md | v1.0 | v1.1 |

**修订后自查**（脚本化，可复跑）：

| 核查项 | 结果 |
| --- | --- |
| 头部版本号 == 变更日志最高版本（13 份文档） | ✅ 全通过 |
| 旧值残留（山水蹇 / 水火未济 / 文档集版本 / 2–4 个 / 失效引用） | ✅ 0 处 |
| Markdown 表格列数一致性 | ✅ 0 处异常 |
| 交叉引用「见 X 文档 §Y」目标章节存在性 | ✅ 0 处失效 |
| 文档用例 → core.js 对拍 | ✅ 32/32 |
| 64 卦三方一致性 | ✅ 全一致 |
| HTML 原型冒烟 | ✅ 25/25 |

> 脚本位于 `.workbuddy/checks/`：`docset-selfcheck.py`（本轮新增）、`doc-case-check.js`、`kb-consistency.py`。

---

## 一、结论摘要

| 维度 | 判定 |
| --- | --- |
| 算法正确性 | ✅ 强。core.js 对文档 32 条用例全过，HTML 原型渲染逐爻正确 |
| 知识库数据 | ✅ 强。64 卦速查表 / 词表 / 代码实现三者完全一致 |
| 文档间一致性 | ❌ 差。互卦更正在 4 处漏改；版本号、章节引用多处漂移 |
| 结构可维护性 | ⚠️ 有隐患。用例表在 03 与 07 各存一份，必然漂移（本次 4 处错误全出自 03 §七） |

**一句话**：这是一套「算法已经对了、文档还没对齐」的文档集。修文档，不用改代码。

---

## 二、P0 缺陷（会直接导致实现错误，必须修）

### P0-1 金标准算例 B 的互卦在 03 文档里仍是错的

03 文档 v1.3 的变更日志声明「更正互卦口径」，但**算例本体没改**：

| 位置 | 现状 | 应为 |
| --- | --- | --- |
| `docs/dev/03-起卦核心算法设计.md:145`（§2.3 算例 B） | 互卦 → **山水蒙** | **水山蹇** |
| `docs/dev/03-起卦核心算法设计.md:331`（§七 TC-B1） | 互卦 **山水蒙** | **水山蹇** |
| `docs/dev/03-起卦核心算法设计.md:337`（§七 TC-H1） | 火地晋 → 互卦 **山水蒙** | **水山蹇** |
| `docs/dev/01-产品需求文档PRD.md:207`（§六 验收 FR-03） | 互卦 **山水蒙** | **水山蹇** |

同文档 §3.4（`03:191`）与 07 文档 §2.1/§2.2（`07:35`、`07:51`）已更正为水山蹇——即**同一份文档内部自相矛盾**。§2.3 是算例本体，被下游（PRD 验收、测试断言）直接引用，危害最大。

### P0-2 UI 设计规范的排盘示意图整块画错

`docs/dev/06-UI-UX设计规范.md` §3.2（`06:77`–`06:84`）以「丙午年八月十五午时」= 算例 A 为例，但示意图与算例 A 对不上：

| 项 | 示意图 | 应为 |
| --- | --- | --- |
| 互卦名 / 上下卦（`06:83-84`） | 山水蒙 / 艮上坎下 | **火泽睽 / 离上兑下** |
| 变卦卦符（`06:80-81`） | 六爻**全阳**（= 乾为天） | 阴 阳 阴 阳 阳 阳（水天需） |
| 动爻圆点（`06:77`） | 落在自上而下**第 2 行**（五爻） | 应落**最下**第 6 行（初爻） |
| 变卦带圆点（`06:81`） | 有 ● | 违反本文件 `06:104`「变卦中对应爻不再标点」 |
| 互卦卦符（`06:78-81`） | 第 4 行阴、末两行为 `─` 占位 | 阳 阴 阳 阴 阳 阳（火泽睽，六爻齐全） |

> 好消息：`docs/ui/观梅-Plumora-UI设计稿.html` **实现是对的**（实测：本卦水风井·圆点在初爻、互卦火泽睽、变卦水天需逐爻全对）。错的只有这份 ASCII 示意图——但 06 是「UI 唯一依据」，示意图错会被后续开发者照抄。

### P0-3 知识库 schema 的 `binary` 字段值与自己的校验规则矛盾

`docs/dev/05-卦象知识库设计.md:77` 水风井示例写 `"binary": 48`。而 §3.4 校验规则 3（`05:128`）定义 `binary == lower.binary | upper.binary << 3`：

```
按规则3：巽(0b110) | 坎(0b010)<<3 = 6 | 16 = 22
按先天数：5 | 6<<3 = 53
48 = 周易卦序（井为第 48 卦）
```

即示例填的是**卦序**，字段名与规则却是**位编码**。二选一：字段改名 `sequence`，或值改为 22。

### P0-4 数据模型的 DAO 示例有语法错误 + 引用不存在的列

`docs/dev/04-数据模型与存储设计.md:70`：

```sql
benGuaName LIKE '%' || kw ORDER BY createdAt DESC     -- 现状：缺 ":" 前缀与闭合引号
benGuaName LIKE '%' || :kw || '%' ORDER BY createdAt DESC   -- 应为
```

且 `benGuaName` 这一列**在 §2.2 实体字段表（`04:36-56`）中根本不存在**——只在 `04:79` 的注里「表结构加一 String 列」。字段表需补 `benGuaName`，否则建表与查询对不上。

---

## 三、P1 缺陷（文档间不一致 / 版本漂移）

### 3.1 知识库文档内部矛盾

| # | 位置 | 问题 |
| --- | --- | --- |
| 1 | `05:95` vs `05:129` | keywords 条数：§3.1 写「2–4 个」，§3.4 与 v1.3 变更日志写「2–5」。§3.1 漏改 |
| 2 | `05:143` | **水火既济只有 1 个关键词**（「已经完成」），违反自己定的 2–5 规则；`core.js` 同样只有 1 个。脚本扫描 64 卦，仅此一例越界 |
| 3 | `05:147` | 注文写「山水蹇」——卦名不存在，应为**水山蹇** |
| 4 | `05:59` | §2.2 八卦类象表 **艮 行只有 10 格，表头 11 格**：静物类象整格丢失，「阻止、静、笃实、保守」错位到静物列。其余 7 行均 11 格 |
| 5 | `05:127` vs `05:70-89` | §3.4 规则 2 提到乾坤的 `yongText` 字段，§3.1 schema 未列出该字段 |

### 3.2 章节交叉引用错误

| # | 位置 | 问题 |
| --- | --- | --- |
| 6 | `03:364` | 「数据规范与完整词表见 **05 文档 §3.1、§3.6**」——05 文档只有 §一~§五，无 §3.6；词表在 **§3.5** |
| 7 | `01:211`、`07:173` | 把「不申请 INTERNET」归到 **NFR-03**（隐私）；按 PRD §四，该条属 **NFR-02**（离线）。`02:28` 写的是「NFR-02/03」，三处口径不一 |

### 3.3 版本号 / 变更日志

| # | 位置 | 问题 |
| --- | --- | --- |
| 8 | `docs/dev/README.md:6` | 「文档集版本 v1.0」，但变更日志已到 v1.4 |
| 9 | `docs/dev/README.md:59-60` | 变更日志 **v1.0 排在 v1.1 之后**，且 v1.1 出现两次（line 58、60）。应合并为一条并倒序 |
| 10 | `手写笔记识读记录.md:5` | 头部「文档版本 v1.0」，变更日志已有 v1.1 |
| 11 | `docs/ui/build/core.js:2` | 头注释「与 03 文档 v1.2 对应」，03 已 v1.3 |

### 3.4 一校结论未回填底稿

`手写笔记识读记录.md` 一校只改了「卦意关键词」列，**「存疑标注」列与 §七 存疑清单仍停留在 AI 初识口径**，出现同格自相矛盾：

| 行 | 关键词列（一校后） | 存疑标注列（未同步） |
| --- | --- | --- |
| `:84` 泽天夬 | 决断、决策、**奇怪** | 取「**决裂**」 |
| `:89` 水风井 | **水牛**、贤德 | 取「**水源**」 |
| `:92` 震为雷 | 震动、**威惧、震慑** | 取「**威严**」 |
| `:114` 地火明夷 | 受伤、**韬光养晦** | 取「**才华被掩**」 |
| `:138` 天雷无妄 | **不要妄想、不要妄念** | Q14 当前取值「**守正**」 |

§七 Q9–Q14 的「当前取值」列整体未随一校更新。

另：`:119` 卦名写作「**水火未济**」——未济为离上坎下，应为 **火水未济**（05 §3.5 是对的）。

### 3.5 功能条目未同步

| # | 位置 | 问题 |
| --- | --- | --- |
| 12 | `06:34-35` | 设置页仅列「笔画标准 / 默认起卦方式 / 主题」，缺 FR-10 v1.1 新增的**体用判定规则**（原型 HTML 已实现该设置，文档滞后） |
| 13 | `03:263-274` | §五 参考实现骨架硬编码「动爻所在为用」，未体现 §3.6/§八 新增的 `ti_yong_rule` 参数（`core.js` 已支持） |
| 14 | `01:105` | FR-05 必含要素仍写「体生用小凶 / 用克体凶」两级，未同步 v1.1 的五级程度徽标（FR-06 已同步） |
| 15 | `04:46` | benGuaCode 示例「**坎巽**或 06-05」，与 §2.5「统一为上卦数-下卦数」矛盾，应删「坎巽」 |
| 16 | `06:117` | 类象类目写「人物 / 人事」，05 §2.2 表头是「人伦 / 性情人事」 |
| 17 | `04:57` | 索引清单列了 `verifyStatus`，但 §2.2 字段表的「约束」列只标了 createdAt 与 benGuaCode |

### 3.6 文档给出的命令跑不起来

`docs/ui/README.md:44` 写「回归测试命令：`node build/smoke.test.js`（需 jsdom，见 Node workspace）」。**按字面执行必然失败**——实测需显式指定 NODE_PATH：

```bash
NODE_PATH="C:/Users/hxzha/.workbuddy/binaries/node/workspace/node_modules" node build/smoke.test.js
```

（且 jsdom 实际装在 `.workbuddy/binaries/`，非 `.workbuddy-ai/binaries/`，README 未写明路径。）

---

## 四、结构性问题（建议改，不是错）

1. **用例表双份维护**：03 §七（摘要）与 07 §三（完整表）内容重叠。本次 4 处互卦错误**全部**出现在 03 §七——因为它没跟着 07 一起升级。建议 03 §七 整节删除，只留一句「完整用例见 07 文档 §三」。
2. **文档集版本无联动机制**：08 §一 只要求「每份文档」更新头部版本与变更日志，没要求同步 README 的「文档集版本」。本次 README 停在 v1.0 就是这么来的。
3. **术语两套**：05 用「人伦 / 性情人事」，06 用「人物 / 人事」；建议统一，并在 README §三 术语速查里收口。
4. **`docs/reference/` 5 张 jpg 共约 28MB** 直接躺在仓库里。若日后入 git，建议压缩或转 webp（原图另有备份的前提下）。

---

## 五、已验证通过的部分（可放心的）

以下均经**实际执行**核对，不是「读着像对的」：

| 验证项 | 方式 | 结果 |
| --- | --- | --- |
| core.js 对文档用例 | 32 条对拍脚本 | ✅ 32/32 通过（含金标准 A/B、TC-N01~04、TC-C01~06、TC-S01、TC-H03/H04、TC-F01~07、TC-LN01、TC-M1/M2、大数不溢出、笔画表） |
| 互卦更正的正确性 | 既济→未济经典验证 | ✅ 算例 A 火泽睽、算例 B 水山蹇，与通行口径一致 |
| HTML 原型渲染 | jsdom 实测算例 A 等价输入（数字 14/5） | ✅ 本卦水风井（圆点在初爻）、互卦火泽睽、变卦水天需，逐爻全对 |
| HTML 原型冒烟 | jsdom 冒烟测试 | ✅ 25/25 通过 |
| 64 卦数据三方一致 | 脚本比对 05 §3.2 表 / 05 §3.5 词表 / core.js HEX64 | ✅ 各 64 条，卦名与上下卦组合全对（含八纯卦），词表逐条一致 |
| 井卦文本 | 逐条比对通行本 | ✅ 卦辞 + 六爻爻辞全对 |
| 八卦爻结构与位编码 | 逐卦核对 | ✅ 8 卦爻结构、binary、五行全对 |
| 年月日时取模 | 手工推演 | ✅ 算例 A 的 S1=30 / S2=37 及余 0 取 8/6 规则自洽 |

---

## 六、待拍板的开放项（文档已标注「待确认」，需你决定）

| # | 事项 | 现状 |
| --- | --- | --- |
| 1 | **一字笔画起卦是否也加秒钟数** | 03 §6.5 标「待确认」；但 07 TC-C03 已按「不加秒」写死断言 —— 文档间口径未收口 |
| 2 | **体用判定默认流派** | 默认 MOVING_LINE（动爻所在为用）；手写笔记主张「上为用下为体」，现做成 `ti_yong_rule` 设置项。笔记作者确认后可考虑调换默认值 |
| 3 | **三条偏离常义的校对结论** | 水风井「水牛」、泽天夬「奇怪」、泽山咸「威即感」—— 建议二校对照原件复核（已在 `knowledge-review/CHANGELOG.md` 留痕） |

---

## 七、修复优先级建议

| 批次 | 内容 | 说明 |
| --- | --- | --- |
| 第 1 批（算法/数据契约） | P0-1 ~ P0-4 | 不改会让实现方直接写错；P0-1 涉及 4 个文件 4 处 |
| 第 2 批（口径收口） | 3.1 表格 5 项 + 3.2 引用 2 项 + 3.3 版本 4 项 | 一次性把版本号与交叉引用刷平 |
| 第 3 批（同步滞后） | 3.4 底稿回填 + 3.5 功能条目 6 项 | 与第 2 批同批做，避免再漂 |
| 第 4 批（结构） | 第四节 4 项 | 消除用例表双份维护这个复发根源 |

---

## 八、核查用到的可复现命令

本次核查留下的两个脚本已存到 `.workbuddy/checks/`，可作后续回归护栏：

```bash
# ① 文档用例 → core.js 实现对拍（32 条断言）
node .workbuddy/checks/doc-case-check.js          # 期望 32 pass / 0 fail

# ② 64 卦三方一致性（05 §3.2 表 / 05 §3.5 词表 / core.js HEX64）
python .workbuddy/checks/kb-consistency.py

# ③ HTML 原型冒烟（jsdom 装在 .workbuddy/binaries/node/workspace，非 .workbuddy-ai）
cd docs/ui && NODE_PATH="C:/Users/hxzha/.workbuddy/binaries/node/workspace/node_modules" \
  node build/smoke.test.js                        # 期望 25 pass / 0 fail
```

> ① 的价值：以后每次改 03/07 文档的用例表，跑一遍就知道文档与实现是否还对齐——本次 4 处互卦漏改正是靠它暴露的。
